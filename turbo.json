{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["src/**", "public/**", "vite.config.*", "package.json", "tsconfig.json"],
      "outputs": ["dist/**", "build/**", "out/**"]
    },
    "lint": {
      "inputs": ["src/**", ".eslintrc*", "package.json"]
    },
    "test": {
      "inputs": ["src/**", "vitest.config.*", "jest.config.*", "package.json"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
