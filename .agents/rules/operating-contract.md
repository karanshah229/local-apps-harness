# Workspace operating contract

Read and follow the root `AGENTS.md` before planning or changing this workspace. Treat it as the single source for audience, product boundaries, connected changes, preferred stack, approvals, and secrets. Use `platform/apps.json` to resolve applications; verify actual references before editing.
