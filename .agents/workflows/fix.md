---
description: Diagnose a broken or slow application and attempt bounded recovery
---

Use `diagnose-and-repair`, then `test-user-journeys` and `verify-connected-change` for any fix. Preserve or restore the last working checkpoint after failed approaches.
