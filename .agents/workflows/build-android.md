---
description: Build and verify an installable Android application
---

Use `build-expo-mobile`, `build-android-release`, and `test-user-journeys`. Update artifact metadata in the registry and provide the APK location.
