---
description: Rename an application without stale routes, containers, mobile identity, or data
---

Use `rename-application`, then `verify-connected-change`. Clarify which identities should change, preserve data, and retain route redirects when the URL changes.
