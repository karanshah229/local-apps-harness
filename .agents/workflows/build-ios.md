---
description: Build and verify an iOS application locally, then optionally publish to TestFlight
---

Use `build-react-native-expo` (or `build-native-ios`), `build-ios-release`, and `test-user-journeys`. Verify the simulator and local signed artifact. Ask for approval immediately before TestFlight upload.
