---
description: Deploy a validated application privately with backup and rollback
---

Use `backup-and-restore`, `verify-connected-change`, and `deploy-and-rollback`. Ask for approval immediately before changing the live app. Verify the registered access URL and roll back automatically if health or acceptance checks fail.
