---
description: Change an application's private base path and every dependent route
---

Use `plan-connected-change` with `route`, `change-application`, `manage-nginx-routing`, `test-user-journeys`, and `verify-connected-change`. Preserve a redirect from the old path unless the user explicitly rejects it.
