---
description: Undo a validated local product change and verify the restored behavior
---

Use `manage-local-git` to identify the product checkpoint and consequences, revert it without destructive reset, then use `test-user-journeys` and `verify-connected-change` on the restored state.
