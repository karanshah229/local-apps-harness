---
description: Implement a requested change and all connected technical effects
---

Use `understand-product-request`, `plan-connected-change`, `change-application`, `test-user-journeys`, and `verify-connected-change`. Preserve a local checkpoint and keep unrequested visible behavior excluded.
